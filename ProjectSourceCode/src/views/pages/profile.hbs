<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Profile</title>
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
        <div style="width: 80%; margin: 40px auto;">
            <table style="width: 100%; border-collapse: collapse; background-color: #fff; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);">
                <tbody>
                    <tr style="border-bottom: 1px solid #ddd;">
                        <h1 style="text-align: center;"><b>Profile</b></h1>
                        <td style="padding: 15px; text-align: left;">
                            <div class="container mt-5">
                                <div class="row justify-content-center">
                                    <div class="col-md-6">
                                        <form action="/___" method="POST">  {{!-- update form action --}}
                                            <div class="mb-3">
                                                <h4>Settings</h4>
                                                <label for="password" class="form-label">Change Password</label>
                                                <input type="password" class="form-control" id="password" name="password">
                                                <label for="goals" class="form-label">Goals</label>
                                                <input type="text" class="form-control" id="goals" name="goals">
                                            </div>
                                            <button class="btn btn-primary" type="submit">Update</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </td>
                        <td style="padding: 15px; text-align: left;">
                            <div class="container mt-5">
                                <div class="row justify-content-center">
                                    <div class="col-md-6">
                                        <form action="/___" method="POST"> {{!-- update form action --}}
                                            <div class="mb-3">
                                                <h4>Favorite Recipes</h4>
                                                <div style="width:150px; height:150px;line-height:1em; overflow:auto; padding:5px;"> {{!-- populate with favorite recipes --}}
                                                    This 'div' element contains more content than the previous one. Because there's too much text to fit into the box, the box grows scrollbars.
                                                </div>
                                                <label for="password" class="form-label">Remove Favorite Recipe</label>
                                                <input type="text" class="form-control" id="remove_favorite_recipe" name="remove_favorite_recipe">
                                            </div>
                                            <button class="btn btn-primary" type="submit">Remove</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>